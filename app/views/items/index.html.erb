<h1>items list</h1>
  <% @items.each do |item| %>
  <div>
    <p><%= link_to item.name, item_path(item) %></p>
    <p> <%= item.price %> </p>
  </div>
  <% end %>


<%= link_to 'New item', new_item_path %>
